const User = require('./User'),
      Group = require('./Group'),
      CheckForm = require('./CheckForm'),
      Check = require('./Check'),
      UserGroup = require('./User-Group')

const Models = {
  User,
  Group,
  Check,
  CheckForm,
  UserGroup
}

module.exports = Models;
